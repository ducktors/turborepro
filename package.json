{
  "private": true,
  "scripts": {
    "build": "turbo build --color",
    "build:server1": "TURBO_API=https://trrc.ducktors.dev turbo build --force --color",
    "build:server2": "TURBO_API=https://turbo.ducktors.dev turbo build --force --color",
    "build:server3": "TURBO_API=https://do-trrc.ducktors.dev turbo build --force --color",
    "precommit": "pnpm install",
    "preinstall": "npx only-allow pnpm"
  },
  "packageManager": "pnpm@10.6.5",
  "devDependencies": {
    "turbo": "^2.4.4"
  }
}
